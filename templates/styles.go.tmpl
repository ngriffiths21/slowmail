:root {
    font-family: 'Franklin Gothic', Avenir, sans-serif;
}

* {
    margin: 0px;
}

body {
    max-width: 50rem;
    margin-left: auto;
    margin-right: auto;
    line-height: 1.5;
}

h1 {
    margin-top: 20px;
    margin-bottom: 20px;
}

input, button, textarea, select {
  font: inherit;
}

input, button, textarea, select {
    margin-top: 0.5rem;
    margin-bottom: 0.5rem;
}

label {
    display: block;
}

div.sign-in-form {
    max-width: 30rem;
    margin-left: auto;
    margin-right: auto;
}